default:
    path_options:
        print_absolute_paths: true
with_editor_url:
    path_options:
        editor_url: phpstorm://open?file={relPath}&line={line}
with_remove_prefix:
    path_options:
        remove_prefix:
            - features/bootstrap/
            - features/
